import 'package:flutter/material.dart';

class GraficasPage extends StatelessWidget {
  const GraficasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Graficos page"),
    );
  }
}
